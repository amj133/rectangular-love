class LoveRectangle {
  constructor(startX, startY, width, height) {
    this.startX = startX;
    this.startY = startY;
    this.width = width;
    this.height = height;
  }
}

module.exports = LoveRectangle;
